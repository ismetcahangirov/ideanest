org.gradle.caching=true
org.gradle.parallel=true

# Gradle's own JVM. The build compiles with a Java 21 toolchain regardless of
# the JDK that launches it, so this only sizes the daemon.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
