rootProject.name = "ideanest-api"

// The service is a modular monolith: one Gradle project, module boundaries
// expressed as packages under `az.ideanest`. Splitting a module into its own
// Gradle subproject is a decision to make when a module is actually extracted,
// not before.
