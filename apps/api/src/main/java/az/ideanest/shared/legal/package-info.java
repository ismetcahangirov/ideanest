/**
 * The vocabulary the legal gates speak, published where the gating modules may name it.
 *
 * <p>{@code shared.access}'s argument, applied to a fourth kind of question. The project
 * module refuses a submission and the pledge module refuses a confirmation, both on the
 * strength of a row in {@code document_acceptances}; neither may read that table or name
 * the module that owns it. So the question lives here and
 * {@code legal.application.LegalAgreements} answers it.
 *
 * <p>Nothing here holds text. A document's body is the legal module's, and #439's routes
 * are what render it.
 */
package az.ideanest.shared.legal;
