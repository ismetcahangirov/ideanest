/**
 * What an update is, and the rules that hold whoever asked for one.
 *
 * <p>Nothing here knows it is stored in PostgreSQL or reached over HTTP, which is what
 * lets {@code UpdateContent}'s bounds be tested without a container.
 */
package az.ideanest.community.domain;
