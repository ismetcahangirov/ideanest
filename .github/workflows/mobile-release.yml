name: Mobile release

# Builds and distributes `apps/mobile` — issue #116.
#
# WHY THIS IS A SEPARATE WORKFLOW FROM `release.yml`.
#
# That one builds container images and deploys a digest, and its central property
# is that nothing is built twice: staging and production run the same bytes, and
# a rollback is a redeploy of yesterday's digest. A mobile build cannot have that
# property. A store build is signed for the store and an internal build is not,
# so the artefact that testers approve is never the artefact that ships — the two
# differ in the one way that matters to Apple and Google. Pretending otherwise by
# sharing a workflow would make the difference invisible rather than absent.
#
# WHAT IS DELIBERATELY NOT HERE: a build.
#
# Compiling a React Native application needs Xcode and the Android SDK, and this
# repository owns neither a macOS runner nor the signing material. `eas build`
# runs on Expo's infrastructure and holds the credentials, which is what EAS is
# for. So every job below either checks something this repository can check, or
# asks EAS to do the work AND SAYS SO AND STOPS when no token is configured —
# the same rule `release.yml`'s deploy step, the metrics endpoint and the cache
# invalidator all follow. A workflow that failed on every push because a secret
# nobody has set is missing is a workflow people learn to ignore.

on:
  push:
    branches: [main]
    paths:
      - 'apps/mobile/**'
      - 'packages/**'
      - '.github/workflows/mobile-release.yml'
  pull_request:
    branches: [main]
    paths:
      - 'apps/mobile/**'
      - '.github/workflows/mobile-release.yml'
  # A store build is a deliberate act with a human behind it. It is never a
  # consequence of merging.
  workflow_dispatch:
    inputs:
      profile:
        description: Which eas.json build profile to run
        required: true
        type: choice
        options: [preview, production]
      submit:
        description: Submit the finished build to the stores
        required: false
        type: boolean
        default: false

concurrency:
  # One mobile build at a time per ref. Two overlapping ones burn two EAS build
  # minutes to produce one artefact anybody wants.
  group: mobile-release-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: '22'

jobs:
  # ---------------------------------------------------------------------------
  # What this repository can check on its own, on every change.
  #
  # A separate job from the `frontend` one in ci.yml, and named separately, so
  # that a mobile failure says "mobile" on the pull request rather than being
  # one line inside a check about Storybook. It repeats the install because a
  # job is a fresh machine; the pnpm cache is what makes that cheap.
  # ---------------------------------------------------------------------------
  check:
    name: Mobile (typecheck, test, config)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          run_install: false

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm --filter @ideanest/mobile typecheck

      - name: Test
        run: pnpm --filter @ideanest/mobile test

      # `app.config.ts` reads two environment variables and THROWS on a value it
      # cannot use, which is deliberate (see the file). Resolving it here is what
      # turns that throw into a failed check rather than into a failed build
      # twenty minutes into an EAS queue.
      - name: Resolve the Expo configuration
        env:
          IDEANEST_API_ORIGIN: https://api.ideanest.az
          IDEANEST_SITE_URL: https://ideanest.az
        run: pnpm --filter @ideanest/mobile exec expo config --type public > /dev/null

      # The two files that let a link open the application (#114) name the same
      # bundle identifier and package that `app.config.ts` does. They are
      # generated from environment variables at run time, so nothing checks that
      # they agree — except this.
      - name: Check the association identifiers agree
        env:
          IDEANEST_SITE_URL: https://ideanest.az
        run: node apps/mobile/scripts/check-association.mjs

  # ---------------------------------------------------------------------------
  # The build, on Expo's infrastructure.
  # ---------------------------------------------------------------------------
  build:
    name: EAS build (${{ inputs.profile || 'preview' }})
    runs-on: ubuntu-latest
    needs: check
    if: github.event_name == 'workflow_dispatch'
    timeout-minutes: 30
    environment: mobile
    steps:
      - uses: actions/checkout@v7
        with:
          # `eas.json` sets `requireCommit`, so the build is identified by the
          # commit that produced it. A shallow checkout with no history is one
          # EAS refuses.
          fetch-depth: 0

      - uses: pnpm/action-setup@v6
        with:
          run_install: false

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Say so and stop when EAS is not configured
        id: eas
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "::notice::EXPO_TOKEN is not set; no build was requested. See apps/mobile/README.md."
            echo "configured=false" >> "$GITHUB_OUTPUT"
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        if: steps.eas.outputs.configured == 'true'
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        # `--non-interactive` because a prompt on a runner is a job that times
        # out an hour later with nothing to show. `--no-wait` is deliberately
        # NOT passed: a workflow that queues a build and reports success has
        # reported on the queue rather than on the build.
        run: pnpm dlx eas-cli@latest build --platform all --profile "${{ inputs.profile }}" --non-interactive

      # Staged rollout — §19.2's shape, as the stores express it. `eas.json`
      # submits Android to the `internal` track as a draft, which is what makes
      # the rollout a decision somebody takes in Play Console rather than a
      # consequence of this workflow finishing.
      - name: Submit
        if: steps.eas.outputs.configured == 'true' && inputs.submit
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: pnpm dlx eas-cli@latest submit --platform all --profile "${{ inputs.profile }}" --non-interactive
